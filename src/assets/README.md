# Assets

Place brand and identity imagery here:

- `coat-of-arms.svg` — Ghana Coat of Arms
- `logo.svg` — Dodow Amanmuo logo
- `safety-shield.svg` — verification/trust badge iconography

These are omitted from the scaffold; drop the real files in and import them where needed (e.g. `Layout.tsx`, `AnonymityBanner.tsx`).
